# OpenAI API Key
OPENAI_API_KEY=your-api-key-here

# Optional: Whisper API Model (default is 'whisper-1')
# WHISPER_MODEL=whisper-1

# Optional: Maximum chunk size in MB (default is 24)
# MAX_CHUNK_SIZE_MB=24

# Optional: Test mode duration in seconds (default is 600, which is 10 minutes)
# TEST_MODE_DURATION=600